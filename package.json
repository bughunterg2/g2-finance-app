{
  "name": "g2-finance-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev"
  },
  "engines": {
    "node": "22.x"
  }
}

